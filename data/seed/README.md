# Demonstration dataset

The deterministic seed fixture lives in `apps/api/app/db/seed.py` so it can be loaded transactionally against the domain model. Values are synthetic and carry `seed_demo` evidence. They must never be marketed as experimental results.
